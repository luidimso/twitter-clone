<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="utf-8">
    <title>Twitter</title>

    <style>
      .field{
        width: 40%;
        margin: 1%;
        float: left;
      }

      .actions{
        width: 14%;
        margin: 1%;
        float: left;
      }

      #user_email, #user_password{
        width: 100%;
        height: 45px;
      }

      #getin{
        width: 100%;
        height: 45px;
        background-color: white;
        border: solid 1px #1da1f2;
        border-radius: 25px;
        color: #1da1f2;
        font-weight: bold;
        transition: 1s all;
      }

      #getin:hover{
        background-color: #eaf5fd;
      }

      #singup{
        height: 40px;
        background-color: #1da1f2;
        color: white;
        font-weight: bold;
        border-radius: 25px;
        transition: 1s all;
      }

      #singup:hover{
        background-color: #006dbf;
      }
    </style>
  </head>

  <body>
    <div class="container">
      <div class="col-lg-6 col-lg-offset-3">
        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="field">
            <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "E-mail" %>
          </div>

          <div class="field">
            <%= f.password_field :password, autocomplete: "current-password", placeholder: "Senha" %>
          </div>

          <div class="actions">
            <%= f.button "Entrar", id: "getin"%>
          </div>
        <% end %>
      </div>
    </div>

    <div class="container">
      <div class="col-lg-4 col-lg-offset-4">
        <h2> <strong>Veja o que está acontecendo no mundo agora</strong> </h2>

        <h3> <strong>Participe hoje do twitter</strong> </h3>
        <br>

        <form class="form">
          <%= render "devise/shared/links" %>
        </form>
      </div>
    </div>
  </body>
</html>
