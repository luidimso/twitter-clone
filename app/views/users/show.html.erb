<h1>Perfil de <%= @user.email %></h1>

<% if current_user.following? @user %>
  <%= link_to "Deixar de seguir", relationship_path(@user), method: :delete %>
<% else %>
  <%= link_to "Seguir", relationships_path(id: @user.id), method: :post %>
<% end %>

<h2>Tweets (<%= @user.tweets.count %>)</h2>
<% if @user.tweets.empty? %>
  <p>Usuário ainda não tem tweets</p>

<% else %>
  <ul>
    <% @user.tweets.each do |tweet| %>
      <li><%= tweet.content %></li>
    <% end %>
  </ul>
<% end %>
